<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body> 
    글 임시 내역
    <table border=1>
        <tr>
            <th>번호</th>
            <th>제목</th>
            <th>내용</th>
            <th>날짜</th>
        </tr>

        <% list s.forEach(function(item) { %>

         <tr>
            <td><%= item.No %></td>
            <td><%= item.Title %></td>
            <td><%= item.content %></td>
            <td><%= item.regdate %></td>
            
         </tr>   
         
        <% }) %> 
    </table>
</body>
</html>